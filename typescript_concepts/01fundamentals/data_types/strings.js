"use strict";
(() => {
    var _a;
    const batman = "Batman";
    const superman = "Superman";
    const flash = `flash`;
    batman.toUpperCase();
    console.log(`I'm ${batman}`);
    console.log(((_a = batman[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'No present');
})();
