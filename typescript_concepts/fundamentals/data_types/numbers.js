"use strict";
() => {
    let avengers = 10;
    const villains = 20;
    if (avengers < villains) {
        console.log('We have problems');
    }
    else {
        console.log('We are save');
    }
    //avengers = Number('55A'); //NaN is a number type
};
